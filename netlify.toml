[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Official Next.js runtime — handles App Router, route handlers (/api/lockers),
# ISR/revalidate, and the security headers from next.config.ts.
[[plugins]]
  package = "@netlify/plugin-nextjs"
